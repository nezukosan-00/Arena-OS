# ArenaOS

ArenaOS is a futuristic operations dashboard for live event venues, combining navigation, accessibility support, and staff coordination in a single experience.

## Development

- Install dependencies with `npm install`
- Start the app with `npm run dev`
- Build for production with `npm run build`
